---
sudo: required
language: minimal
services:
  - docker
before_install:
  - sudo apt-get install ansible
  - sudo echo -e "localhost ansible_connection=local\n" | sudo tee /etc/ansible/hosts
install:
  - pwd
  #- pip install molecule
  #- pip install required driver (e.g. docker, python-vagrant, shade, boto, apache-libcloud)
script:
  - ANSIBLE_ROLES_PATH=. ansible-playbook --sudo -D tests/test.yml
